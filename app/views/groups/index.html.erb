
<h1>ORGANISATION</h1>
<div class="jumbotron">
  <div class="container">
<h2>Listing Groupes</h2>


<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Salle</th>
      <th>Tache</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @groups.each do |group| %>
      <tr>
        <td><%= group.name %></td>
        <td><%= group.salle %></td>
        <td><%= group.tache %></td>
        <!-- <td><%= link_to 'Show', group %></td> -->
        <td><%= link_to 'Edit', edit_group_path(group) %></td>
        <td><%= link_to 'Destroy', group, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% group.people.each do |person| %>

    <tr>
        <td><%= person.name %></td>

        <td></td>
        <td></td>
        <td></td>
        <td></td>
    </tr>
    <% end %>
    <% end %>

  </tbody>
</table>

<br>
<div class="btn btn-default">
<%= link_to 'New Group', new_group_path %>
</div>
</div>
</div>


<div class="jumbotron">
  <div class="container">

<p id="notice"><%= notice %></p>
 <div class="row">
     <div class="col-md-6 ligne">
<h2>Listing People</h2>
<div class="btn btn-default col-md-6 ligne">
    <%= link_to 'Random', randomGroup_path %>
</div>
</div>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <!-- <th>Email</th>
      <th>Tel</th> -->
      <th>Group</th>
      <th>Sensei</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @people.each do |person| %>
      <tr>
        <td><%= person.name %></td>
        <!-- <td><%= person.email %></td>
        <td>0<%= person.tel %></td> -->
        <td><%= person.group.name unless person.group.nil? %></td>
        <td><%= person.sensei %></td>
        <!-- <td><%= link_to 'Show', person %></td> -->
        <!-- <td><%= link_to 'Edit', edit_person_path(person) %></td>
        <td><%= link_to 'Destroy', person, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

</div>
</div>
